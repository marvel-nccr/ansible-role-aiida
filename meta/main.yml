---
allow_duplicates: no

galaxy_info:
  author: "MARVEL NCCR"
  description: "An ansible role that installs and configures [AiiDA](http://www.aiida.net/) on Ubuntu."
  license: MIT
  min_ansible_version: 2.5
  platforms:
    - name: Ubuntu
      versions:
      - xenial
      - bionic
  galaxy_tags:
    - ubuntu
    - application
    - science

dependencies:
- role: marvel-nccr.current-user
  version: master
