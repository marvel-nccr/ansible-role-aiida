label: "{{ aiida_computer_name }}"
hostname: "{{ aiida_computer_name }}"
transport: "{{ aiida_computer_transport }}"
scheduler: "{{ aiida_computer_scheduler }}"
work_dir: "{{ aiida_computer_run_folder_abs }}"
mpirun_command: "{{ aiida_computer_mpirun_command }}"
mpiprocs_per_machine: "{{ aiida_computer_cpus }}"
